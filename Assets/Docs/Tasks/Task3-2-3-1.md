# Task ID: 3.2.3.1
# Parent Task ID: 3.2.3
# Title: Implement PatrolPath Component
# Status: pending
# Dependencies: None (simple MonoBehaviour)
# Priority: high (dependency for PatrolState)
# Estimated Effort: S
# Assignee: Unassigned

# Description:
Implement a simple `PatrolPath.cs` MonoBehaviour component that holds an array of `Transform` waypoints for an enemy's patrol behavior.

# Details:
1.  **File Location:** `Assets/_Project/Scripts/AI/Components/PatrolPath.cs` (Create `Components` subfolder under `AI`)
2.  **Namespace:** `PetalsOfHope.AI.Components`
3.  **Implementation:**
    ```csharp
    // In Assets/_Project/Scripts/AI/Components/PatrolPath.cs
    namespace PetalsOfHope.AI.Components
    {
        using UnityEngine;

        public class PatrolPath : MonoBehaviour
        {
            [Tooltip("Array of waypoints for patrolling. Enemy will move between these transforms.")]
            public Transform[] waypoints;

            private void OnDrawGizmosSelected()
            {
                if (waypoints == null || waypoints.Length < 2)
                {
                    return;
                }

                Gizmos.color = Color.green;
                for (int i = 0; i < waypoints.Length; i++)
                {
                    if (waypoints[i] == null) continue;

                    Gizmos.DrawSphere(waypoints[i].position, 0.3f); // Draw sphere at waypoint

                    if (i < waypoints.Length - 1) // Draw line to next waypoint
                    {
                        if (waypoints[i+1] != null)
                            Gizmos.DrawLine(waypoints[i].position, waypoints[i + 1].position);
                    }
                    else // Draw line from last to first to close the loop (optional visual)
                    {
                         if (waypoints[0] != null)
                            Gizmos.DrawLine(waypoints[i].position, waypoints[0].position);
                    }
                }
            }
        }
    }
    ```

# Acceptance Criteria:
- `PatrolPath.cs` MonoBehaviour is created.
- It contains a public array `Transform[] waypoints`.
- `OnDrawGizmosSelected` is implemented to visualize the waypoints and path in the Scene editor when the GameObject is selected.
- Script compiles without errors.

# Test Strategy:
- Manual Verification:
    - Create an empty GameObject in a scene.
    - Attach `PatrolPath.cs` component.
    - In the Inspector, assign several child Transforms (or other scene Transforms) to the `waypoints` array.
    - Verify that Gizmos draw spheres at each waypoint and lines connecting them in the Scene view when the GameObject is selected.

# Notes/Questions:
- This component is purely for data and editor visualization. The logic for using these waypoints resides in `PatrolState.cs`.
- The Gizmos help designers set up patrol paths visually.